# include <stdio.h>
 int main()
 {
 	int a,b,sum,diff;
 	printf("enter a and b values ");
 	scanf("%d%d",&a,&b);
 	sum=a+b;
	diff=a-b;
	printf("\nsum is%4d",sum);
	printf("\ndifference iS%4d",diff);
	 return 0;
	  }
